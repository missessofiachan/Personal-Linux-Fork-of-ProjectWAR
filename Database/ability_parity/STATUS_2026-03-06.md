# Ability Parity Status Checkpoint (2026-03-06)

## Scope
This checkpoint captures the current Mythic ability parity path across:
- Database import/schema and parity scripts (`Database/ability_parity/*`)
- Core ability loading/translation in server and shared DB models (`Common/*`, `WorldServer/*`)

## Completed / Landed in Branch
- Added Mythic CSV and source-table plumbing for ability/effect data.
- Added resolver pipeline artifacts (`ability_entry_resolver` schema + build/sync scripts).
- Added naming-policy documentation and CSV-first naming aliases in schema/import/code.
- Added runtime switches and loaders for mythic source tables in server startup paths.
- Added first pass of mastery cap handling and RR mastery tactic compatibility work.
- Added first pass of renown-point clamping and renown state rebuild safeguards.
- Added battlefront startup reset controls and associated campaign manager hooks.

## Current Known Regressions (Not Fully Resolved)
1. Mastery UI plus button lockout:
- The `+` button still does not consistently hard-disable at exactly 15 invested points in all client-side confirm/non-confirm states.
- Additional packet-state parity work is still required for pre-confirm vs post-confirm mastery state transitions.

2. Renown investment behavior:
- Renown investment/removal behavior is still not fully stable in live testing.
- Overflow and chart reconciliation paths were patched but require another correctness pass against live behavior expectations.

3. Renown tactics equip behavior:
- User reports inability to equip two renown tactics in current build.
- Current validity fallback is not sufficient; tactic-source classification and slot validation need another pass.

## Database Target For Runtime Testing
- Runtime world server testing should target the schema configured in `World.xml`.
- For this parity workstream, use `war_world_curated` as the test world schema unless intentionally validating a separate local schema.

## Next Required Work
1. Mastery packet parity audit:
- Compare `F_CAREER_CATEGORY`, `F_CAREER_PACKAGE_INFO`, and `F_CAREER_PACKAGE_UPDATE` transitions for tree state before/after confirmation.

2. Renown model hardening:
- Enforce monotonic non-negative accounting for points spent/available across all purchase/respec/load paths.
- Rebuild trained-state from saved string once per transaction and validate slot prerequisites deterministically.

3. Renown tactic eligibility audit:
- Separate mastery-tree tactics from renown-granted tactics using explicit source mapping.
- Validate equip slot count and eligibility independently from mastery tree unlock states.
